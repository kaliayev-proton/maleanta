---
import { siteConfig } from "../config/site-config";
---

<header class="border-mal-accent p-5 border-t-2 border-b-2 mb-2 mt-2">
  <h1 class="uppercase type-writer text-mal-font">
    {[...siteConfig.title].map((char) => <span>{char}</span>)}
  </h1>
</header>
